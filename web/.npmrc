# Painel web usa pacotes que não estão no registry interno do ML.
# Execute npm install fora da rede corporativa (casa, hotspot, VPN).
#
# Alternativa rápida no terminal:
#   npm_config_registry=https://registry.npmjs.org npm install
registry=https://registry.npmjs.org
